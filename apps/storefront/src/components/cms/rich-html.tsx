/**
 * Re-exported from `@ecom/ui` (Sprint 11 Storybook pass) — `RichHtml` has no
 * storefront-specific dependencies, so it now lives alongside the other
 * shared presentational primitives where it can be storied in Storybook.
 * Kept as a local re-export so existing imports from
 * `@/components/cms/rich-html` continue to work unchanged.
 */
export { RichHtml } from "@ecom/ui";
