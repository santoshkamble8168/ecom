# Root-level convenience file for `docker compose --env-file`.
# Service-specific settings live in apps/api/.env.example and
# infrastructure/docker/.env.example. Copy each to a real .env before
# running the platform.

POSTGRES_USER=ecom
POSTGRES_PASSWORD=ecom
POSTGRES_DB=ecom

MEILI_MASTER_KEY=devMasterKeyChangeMe

MINIO_ROOT_USER=ecomminio
MINIO_ROOT_PASSWORD=ecomminio123
